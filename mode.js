function mode(arr){

// given an array of numbers, return
// the most frequent number within the list

}

mode([1,3,5,6,3]); // --> 3
mode([1]); // --> 1


/*

  Going further, refactor your solution so that if an array has
  2 modes, it will return an array with those modes.

  **see function modeAnswerExtra
*/

mode([1,1,1,5,5,5]); // --> [1,5]
